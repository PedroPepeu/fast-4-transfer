# Docker

## Setting up

## env-files

## Running
`podman-compose up --build`
`podman-compose down`
`podman-compose up`

## Ports
**Frontend** -> http://localhost:4321
**Backend** -> http://localhost:8080/[route]
**IPFS API** -> http://localhost:5001/webui
