# Architecture

## Input
Files uploaded to *IPFS*.

## Middle
The files will be hashed and send to *IPFS* network, the link returned will become an QR code and will be made an link shortener so no one can track.

## Output
QR code (and/or link) so the user can download.

